# services
Non docker/compose/swarm apps and services that may need more configuration
These can be repositories or static files

_add the deployment steps to a script file or a runner and add to crontab_
