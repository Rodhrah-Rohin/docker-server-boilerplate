# runners
a bunch of files used to setup and run services and stacks properly
consists by default of compose stacks runner and the stacks runner

## compose.sh
configures and runs all setup docker compose files

## stack.sh
configures and runs all setup docker swarm files
